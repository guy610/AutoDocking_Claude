---
phase: 02-input-collection-ui
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/fto_agent/main_window.py
  - src/fto_agent/app.py
  - tests/test_input_panel.py
  - tests/test_smiles_validator.py
autonomous: false

must_haves:
  truths:
    - "User can see input panel when application launches"
    - "User can enter problem description in multi-line text field"
    - "User can enter solution/active in multi-line text field"
    - "User can enter constraints in text field"
    - "User can paste SMILES and see validation feedback (valid/invalid)"
    - "User can select multiple countries from checkbox list"
    - "Submit button is disabled until required fields are filled"
  artifacts:
    - path: "src/fto_agent/main_window.py"
      provides: "MainWindow with InputPanel as central widget"
      contains: "InputPanel"
    - path: "tests/test_input_panel.py"
      provides: "Unit tests for InputPanel widget"
      contains: "def test_"
    - path: "tests/test_smiles_validator.py"
      provides: "Unit tests for SMILES validation"
      contains: "def test_"
  key_links:
    - from: "src/fto_agent/main_window.py"
      to: "src/fto_agent/widgets/input_panel.py"
      via: "import and instantiation"
      pattern: "from fto_agent\\.widgets import.*InputPanel"
    - from: "src/fto_agent/app.py"
      to: "VALIDATION_STYLESHEET"
      via: "stylesheet application"
      pattern: "setStyleSheet"
---

<objective>
Integrate InputPanel into MainWindow and create comprehensive unit tests.

Purpose: Completes Phase 2 by connecting the InputPanel widget to the application and verifying all requirements through tests. User will be able to launch the app and see the complete input form.

Output: Working application with input panel and passing unit tests for all input collection requirements.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-input-collection-ui/02-RESEARCH.md
@.planning/phases/02-input-collection-ui/02-01-SUMMARY.md
@src/fto_agent/main_window.py
@src/fto_agent/app.py
@tests/test_workers.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate InputPanel into MainWindow</name>
  <files>
    src/fto_agent/main_window.py
    src/fto_agent/app.py
  </files>
  <action>
1. Update src/fto_agent/main_window.py:
   - Import InputPanel from fto_agent.widgets
   - In _setup_central_widget():
     - Replace the demo content (status_label, demo_button, layout.addStretch) with InputPanel
     - Create self._input_panel = InputPanel()
     - Add input_panel to the layout
     - Connect input_panel.submitRequested to a new _start_fto_search method
   - Remove _demo_work function (no longer needed)
   - Remove _start_demo_operation method (no longer needed)
   - Update _start_fto_search to:
     - Get data via self._input_panel.get_data()
     - Show status bar message: "FTO search not yet implemented"
     - (Actual search will be implemented in Phase 3)
   - Keep the status bar setup, progress bar, cancel button, and progress manager
   - Keep worker-related methods (_cancel_operation, _on_progress, _on_result, _on_error, _on_finished) for future use

2. Update src/fto_agent/app.py:
   - Add VALIDATION_STYLESHEET constant (from 02-RESEARCH.md)
   - In create_app() function, after creating QApplication, call app.setStyleSheet(VALIDATION_STYLESHEET)
   - This enables the visual validation feedback (red/green borders on SMILES field)

VALIDATION_STYLESHEET should include:
- QLineEdit[validationState="error"] - red border, light red background
- QLineEdit[validationState="valid"] - green border
- Default QLineEdit - gray border
- QGroupBox styling for countries section
  </action>
  <verify>
    python -c "from fto_agent.main_window import MainWindow; w=MainWindow(); print(hasattr(w, '_input_panel'))"
    python -c "from fto_agent.app import create_app, VALIDATION_STYLESHEET; print('VALIDATION_STYLESHEET' in dir())"
  </verify>
  <done>
    - MainWindow creates InputPanel as central widget
    - VALIDATION_STYLESHEET defined in app.py and applied to QApplication
    - _start_fto_search method exists and retrieves form data
    - Demo button and demo work function removed
  </done>
</task>

<task type="auto">
  <name>Task 2: Create unit tests</name>
  <files>
    tests/test_input_panel.py
    tests/test_smiles_validator.py
  </files>
  <action>
1. Create tests/test_smiles_validator.py with tests:
   - test_validate_smiles_valid: Valid SMILES (e.g., "CCO", "c1ccccc1") returns is_valid=True
   - test_validate_smiles_invalid: Invalid SMILES (e.g., "not_a_smiles", "XYZ") returns is_valid=False
   - test_validate_smiles_empty: Empty string returns is_valid=True (optional field)
   - test_validate_smiles_whitespace: Whitespace-only returns is_valid=True
   - test_validate_smiles_ghk_peptide: Test with GHK SMILES from requirements (NCC(=O)NC(Cc1cnc[nH]1)C(=O)NC(CCCCN)C(=O)O)
   - test_validate_smiles_palmitoyl_ghk: Test with Pal-GHK SMILES from requirements
   - test_is_rdkit_available: Verify is_rdkit_available() returns True

2. Create tests/test_input_panel.py with tests (use qtbot fixture from pytest-qt):
   - test_input_panel_creation: Can create InputPanel without error
   - test_input_panel_initial_validity: Empty form is invalid
   - test_input_panel_validity_with_required_fields: Form valid when problem, solution filled and country selected
   - test_input_panel_validity_without_countries: Form invalid when no countries selected
   - test_input_panel_get_data: get_data() returns dict with all expected keys
   - test_input_panel_get_data_values: Values in get_data() match what was entered
   - test_input_panel_smiles_validation_valid: Valid SMILES shows green feedback
   - test_input_panel_smiles_validation_invalid: Invalid SMILES shows red feedback
   - test_input_panel_clear: clear() resets all fields to default
   - test_input_panel_submit_signal: submitRequested signal emitted when submit button clicked
   - test_input_panel_validity_changed_signal: validityChanged signal emitted when validity changes

Use qtbot.addWidget(panel) for proper cleanup.
Use qtbot.waitSignal() for signal tests.
  </action>
  <verify>
    pytest tests/test_smiles_validator.py -v
    pytest tests/test_input_panel.py -v
  </verify>
  <done>
    - All SMILES validator tests pass
    - All InputPanel tests pass
    - Tests cover all five input requirements (INP-01 through INP-05)
    - Test cases from requirements (GHK, Pal-GHK SMILES) included
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete input collection UI for FTO queries:
- Problem description field (multi-line)
- Solution/active field (multi-line)
- Constraints field (multi-line)
- SMILES notation with real-time validation (shows valid/invalid)
- Country multi-select (US, EU, CN, JP)
- Submit button that enables when form is valid
  </what-built>
  <how-to-verify>
1. Launch the application:
   ```bash
   cd "C:\Users\User\My Drive (guy@elixrbio.com)"
   python -m fto_agent
   ```

2. Verify the input form is visible with all fields:
   - Problem description (multi-line text area)
   - Solution/Active (multi-line text area)
   - Constraints (multi-line text area)
   - SMILES (single line with status label below)
   - Target Countries (checkboxes for US, EU, CN, JP)
   - Submit button ("Start FTO Search")

3. Test form validation:
   - Submit button should be DISABLED initially
   - Enter text in Problem field
   - Enter text in Solution field
   - Submit button should become ENABLED

4. Test SMILES validation:
   - Paste valid SMILES: `CCO` - should show green "Valid molecule with 3 atoms"
   - Clear and paste invalid: `invalid` - should show red "Invalid SMILES notation"
   - Clear completely - status should clear, form still valid

5. Test country selection:
   - Uncheck all countries - submit button should DISABLE
   - Check at least one country - submit button should ENABLE (if problem/solution filled)

6. Test with test case SMILES from requirements:
   - GHK: `NCC(=O)NC(Cc1cnc[nH]1)C(=O)NC(CCCCN)C(=O)O`
   - Should show as valid

7. Click Submit button - should show status message (search not implemented yet)

8. Verify application remains responsive (no freezes)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:

1. Run full test suite:
   ```bash
   pytest tests/ -v
   ```
   All tests should pass including Phase 1 worker tests.

2. Verify requirements coverage:
   - INP-01: Problem description field exists and accepts input
   - INP-02: Solution field exists and accepts input
   - INP-03: Constraints field exists and accepts input
   - INP-04: SMILES field with validation feedback
   - INP-05: Country multi-select with US, EU, CN, JP

3. Application launches and displays input panel:
   ```bash
   python -m fto_agent
   ```
</verification>

<success_criteria>
1. User can enter problem description in multi-line text field (INP-01)
2. User can enter solution/active description in multi-line text field (INP-02)
3. User can enter constraints in text field (INP-03)
4. User can paste SMILES notation and see validation feedback (INP-04)
5. User can select multiple target countries (INP-05)
6. Submit button enables only when required fields are valid
7. All unit tests pass
8. Human verifies visual appearance and interaction
</success_criteria>

<output>
After completion, create `.planning/phases/02-input-collection-ui/02-02-SUMMARY.md`
</output>
