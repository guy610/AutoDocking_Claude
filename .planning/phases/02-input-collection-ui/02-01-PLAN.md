---
phase: 02-input-collection-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/fto_agent/validators/__init__.py
  - src/fto_agent/validators/smiles.py
  - src/fto_agent/widgets/input_panel.py
  - src/fto_agent/widgets/__init__.py
  - pyproject.toml
autonomous: true

must_haves:
  truths:
    - "SMILES validation returns valid/invalid with message"
    - "Empty SMILES is treated as valid (field is optional)"
    - "InputPanel contains all five input fields"
    - "Form tracks validity state based on required fields"
  artifacts:
    - path: "src/fto_agent/validators/smiles.py"
      provides: "SMILES validation using RDKit"
      exports: ["validate_smiles", "SmilesValidationResult", "is_rdkit_available"]
    - path: "src/fto_agent/widgets/input_panel.py"
      provides: "InputPanel widget with all FTO query inputs"
      exports: ["InputPanel", "COUNTRIES"]
  key_links:
    - from: "src/fto_agent/widgets/input_panel.py"
      to: "src/fto_agent/validators/smiles.py"
      via: "import for SMILES validation"
      pattern: "from fto_agent\\.validators\\.smiles import"
---

<objective>
Create the SMILES validator module and InputPanel widget for collecting FTO query inputs.

Purpose: Provides the core UI building blocks for Phase 2. The SMILES validator enables chemical structure validation using RDKit. The InputPanel widget collects all five input types: problem, solution, constraints, SMILES, and countries.

Output: Working validator module and InputPanel widget ready for MainWindow integration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-input-collection-ui/02-RESEARCH.md
@src/fto_agent/widgets/__init__.py
@src/fto_agent/widgets/progress.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SMILES validator module</name>
  <files>
    src/fto_agent/validators/__init__.py
    src/fto_agent/validators/smiles.py
    pyproject.toml
  </files>
  <action>
1. Add rdkit to pyproject.toml dependencies:
   ```toml
   dependencies = [
       "PySide6>=6.6",
       "rdkit>=2025.9.3",
   ]
   ```

2. Create src/fto_agent/validators/__init__.py:
   - Export validate_smiles, SmilesValidationResult, is_rdkit_available

3. Create src/fto_agent/validators/smiles.py with:
   - SmilesValidationResult dataclass (is_valid: bool, message: str, atom_count: Optional[int])
   - validate_smiles(smiles: str) -> SmilesValidationResult
   - is_rdkit_available() -> bool
   - Graceful handling when RDKit not installed (return result with message)
   - Suppress RDKit stderr logging with RDLogger.DisableLog('rdApp.*')
   - Empty/whitespace SMILES returns valid with empty message (field is optional)
   - Use try/except around Chem.MolFromSmiles for unexpected errors

Follow the exact pattern from 02-RESEARCH.md "Complete SMILES Validator Module" section.
  </action>
  <verify>
    python -c "from fto_agent.validators.smiles import validate_smiles, SmilesValidationResult, is_rdkit_available; print(is_rdkit_available())"
    python -c "from fto_agent.validators.smiles import validate_smiles; r=validate_smiles('CCO'); print(r.is_valid, r.message)"
    python -c "from fto_agent.validators.smiles import validate_smiles; r=validate_smiles('invalid'); print(r.is_valid)"
    python -c "from fto_agent.validators.smiles import validate_smiles; r=validate_smiles(''); print(r.is_valid)"
  </verify>
  <done>
    - validate_smiles returns SmilesValidationResult with is_valid=True for valid SMILES (e.g., "CCO")
    - validate_smiles returns is_valid=False for invalid SMILES (e.g., "invalid")
    - validate_smiles returns is_valid=True for empty string (optional field)
    - is_rdkit_available() returns True when RDKit installed
  </done>
</task>

<task type="auto">
  <name>Task 2: Create InputPanel widget</name>
  <files>
    src/fto_agent/widgets/input_panel.py
    src/fto_agent/widgets/__init__.py
  </files>
  <action>
1. Create src/fto_agent/widgets/input_panel.py with:
   - COUNTRIES constant: list of (code, name) tuples for US, EU, CN, JP
   - InputPanel(QWidget) class with:
     - validityChanged = Signal(bool) - emitted when form validity changes
     - submitRequested = Signal() - emitted when user clicks submit
     - __init__: call _setup_ui(), _connect_signals(), initial _check_validity()
     - _setup_ui(): Create QVBoxLayout containing:
       - QFormLayout with:
         - problem_edit (QPlainTextEdit, required, placeholder with example)
         - solution_edit (QPlainTextEdit, required, placeholder with example)
         - constraints_edit (QPlainTextEdit, optional, placeholder with example)
         - smiles_edit (QLineEdit, optional, with smiles_status QLabel below)
       - country_group (QGroupBox) with QGridLayout of QCheckBox for each country
       - submit_button (QPushButton "Start FTO Search", disabled by default)
     - _connect_signals(): Connect textChanged, stateChanged signals to _check_validity
     - _on_smiles_changed(): Validate SMILES, update smiles_status label (green/red), set validationState property
     - _set_validation_style(): Set property and call unpolish/polish for style update
     - _check_validity(): Update submit_button.setEnabled based on is_valid()
     - is_valid(): Return True if problem, solution have content, at least one country checked, and SMILES (if provided) is valid
     - get_data(): Return dict with keys: problem, solution, constraints, smiles, countries
     - clear(): Reset all fields to default state

2. Update src/fto_agent/widgets/__init__.py:
   - Add: from fto_agent.widgets.input_panel import InputPanel, COUNTRIES

Mark required fields in labels with asterisk: "Problem *:", "Solution/Active *:", "Target Countries *"
Set minimum heights: problem/solution 80px, constraints 60px.
Disable SMILES input if RDKit not available, show orange status message.

Follow the exact pattern from 02-RESEARCH.md "Complete InputPanel Widget" section.
  </action>
  <verify>
    python -c "from fto_agent.widgets import InputPanel, COUNTRIES; print(len(COUNTRIES))"
    python -c "from fto_agent.widgets import InputPanel; p=InputPanel(); print(p.is_valid())"
    python -c "from fto_agent.widgets import InputPanel; p=InputPanel(); p.problem_edit.setPlainText('test'); p.solution_edit.setPlainText('test'); print(p.is_valid())"
  </verify>
  <done>
    - InputPanel can be instantiated without errors
    - COUNTRIES contains 4 entries (US, EU, CN, JP)
    - is_valid() returns False when problem/solution empty
    - is_valid() returns True when problem/solution filled and at least one country checked
    - get_data() returns dict with all expected keys
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. SMILES validation works correctly:
   ```bash
   python -c "from fto_agent.validators.smiles import validate_smiles; print(validate_smiles('NCC(=O)NC(Cc1cnc[nH]1)C(=O)NC(CCCCN)C(=O)O'))"
   ```
   Should show valid result for GHK peptide SMILES from test case.

2. InputPanel can be imported and created:
   ```bash
   python -c "from fto_agent.widgets import InputPanel; p=InputPanel(); print(type(p).__name__)"
   ```

3. Form validation logic works:
   ```bash
   python -c "
from fto_agent.widgets import InputPanel
p = InputPanel()
print('Empty form valid:', p.is_valid())
p.problem_edit.setPlainText('test problem')
p.solution_edit.setPlainText('test solution')
print('Filled form valid:', p.is_valid())
data = p.get_data()
print('Data keys:', list(data.keys()))
print('Countries:', data['countries'])
"
   ```
</verification>

<success_criteria>
1. SMILES validator correctly identifies valid/invalid SMILES notation
2. Empty SMILES is treated as valid (optional field)
3. InputPanel contains all five input sections
4. Form validation requires problem, solution, and at least one country
5. get_data() returns structured dictionary with all inputs
6. rdkit added to pyproject.toml dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/02-input-collection-ui/02-01-SUMMARY.md`
</output>
