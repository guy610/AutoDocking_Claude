---
phase: 03-uspto-patent-search
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/fto_agent/workers/uspto_worker.py
  - src/fto_agent/workers/__init__.py
  - src/fto_agent/widgets/results_panel.py
  - src/fto_agent/widgets/__init__.py
autonomous: true

must_haves:
  truths:
    - "USPTO worker executes search in background thread"
    - "USPTO worker reports progress during search"
    - "USPTO worker supports cancellation"
    - "Results panel displays patent list with title and abstract"
    - "Results panel shows result count"
  artifacts:
    - path: "src/fto_agent/workers/uspto_worker.py"
      provides: "perform_uspto_search, create_uspto_search_worker"
      exports: ["perform_uspto_search", "create_uspto_search_worker"]
    - path: "src/fto_agent/widgets/results_panel.py"
      provides: "ResultsPanel widget for displaying patent results"
      exports: ["ResultsPanel"]
  key_links:
    - from: "src/fto_agent/workers/uspto_worker.py"
      to: "src/fto_agent/services/uspto.py"
      via: "USPTOClient import"
      pattern: "from fto_agent\\.services import"
    - from: "src/fto_agent/workers/uspto_worker.py"
      to: "src/fto_agent/workers/base.py"
      via: "Worker import"
      pattern: "from fto_agent\\.workers import Worker"
---

<objective>
Create the USPTO search worker and results display panel for patent search functionality.

Purpose: Enable background patent search with progress reporting and cancellation, plus UI to display results.

Output: USPTO worker module using existing Worker pattern, ResultsPanel widget showing patent list.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-uspto-patent-search/03-RESEARCH.md
@.planning/phases/03-uspto-patent-search/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create USPTO search worker</name>
  <files>src/fto_agent/workers/uspto_worker.py, src/fto_agent/workers/__init__.py</files>
  <action>
Create the USPTO search worker that uses the existing Worker pattern from Phase 1.

**Create src/fto_agent/workers/uspto_worker.py:**

1. **perform_uspto_search** function (the worker target):
   - Signature: `perform_uspto_search(problem: str, solution: str, constraints: Optional[str], api_key: Optional[str], is_cancelled: Callable[[], bool], progress_callback: Callable[[int, int, str], None]) -> PatentSearchResponse`
   - Step 1 (1/4): Extract keywords using extract_search_terms
   - Check is_cancelled() after each step, return empty PatentSearchResponse if cancelled
   - Step 2 (2/4): Build query using build_keyword_query
   - If no keywords extracted, raise USPTOSearchError("Could not extract keywords from input")
   - Step 3 (3/4): Execute search using USPTOClient
   - Use size=100, sort=[{"patent_date": "desc"}] for most recent first
   - Step 4 (4/4): Return response with progress message showing total_hits count
   - Wrap in try/except to convert exceptions to USPTOSearchError

2. **create_uspto_search_worker** factory function:
   - Signature: `create_uspto_search_worker(data: dict[str, Any], api_key: Optional[str] = None) -> Worker`
   - data comes from InputPanel.get_data() with keys: problem, solution, constraints, smiles, countries
   - Return Worker(perform_uspto_search, problem=data["problem"], solution=data["solution"], constraints=data.get("constraints"), api_key=api_key)

**Update src/fto_agent/workers/__init__.py:**
Add exports for perform_uspto_search and create_uspto_search_worker.

Use the pattern from 03-RESEARCH.md exactly. The worker function receives is_cancelled and progress_callback as kwargs (per Phase 1 Worker pattern).
  </action>
  <verify>
Run: `python -c "from fto_agent.workers import create_uspto_search_worker; print('OK')"` returns OK.
  </verify>
  <done>USPTO worker exists and can be created from InputPanel data dict.</done>
</task>

<task type="auto">
  <name>Task 2: Create results panel widget</name>
  <files>src/fto_agent/widgets/results_panel.py, src/fto_agent/widgets/__init__.py</files>
  <action>
Create a Qt widget to display patent search results.

**Create src/fto_agent/widgets/results_panel.py:**

1. **ResultsPanel(QWidget)** class:

   **Signals:**
   - patentSelected = Signal(str) - Emitted when user clicks a patent, passes patent_id

   **__init__(self):**
   - Create layout (QVBoxLayout)
   - Add header label: "Search Results" (bold)
   - Add results count label: "No results" (initially)
   - Add scroll area with QListWidget for patent list
   - Style list items for readability

   **set_results(self, response: PatentSearchResponse):**
   - Update count label: f"Found {response.total_hits} patents (showing {response.count})"
   - Clear existing list items
   - For each patent in response.patents:
     - Create QListWidgetItem with patent_title as text
     - Set tooltip with patent_abstract (truncated to 300 chars if longer)
     - Store patent_id in item.data(Qt.UserRole)
   - If no results, show "No patents found matching your query"

   **set_loading(self, loading: bool):**
   - If loading: show "Searching..." in count label, disable list
   - If not loading: enable list

   **set_error(self, message: str):**
   - Update count label: f"Error: {message}"
   - Clear list items

   **clear(self):**
   - Reset to initial state ("No results", empty list)

   **_on_item_clicked(self, item: QListWidgetItem):**
   - Emit patentSelected signal with patent_id from item.data(Qt.UserRole)

**Update src/fto_agent/widgets/__init__.py:**
Add ResultsPanel to exports.

Style the widget for usability:
- Patent titles should be readable (adequate font size)
- Abstract in tooltip provides detail on hover
- Clear visual feedback for loading/error states
  </action>
  <verify>
Run: `python -c "from fto_agent.widgets import ResultsPanel; print('OK')"` returns OK.
  </verify>
  <done>ResultsPanel widget exists and displays patent results with title, count, and abstract tooltip.</done>
</task>

</tasks>

<verification>
1. Worker module imports work:
   ```bash
   python -c "from fto_agent.workers import Worker, create_uspto_search_worker, perform_uspto_search; print('Worker imports OK')"
   ```

2. ResultsPanel widget imports work:
   ```bash
   python -c "from fto_agent.widgets import ResultsPanel; print('ResultsPanel import OK')"
   ```

3. Worker creation works with mock data:
   ```bash
   python -c "
from fto_agent.workers import create_uspto_search_worker
data = {'problem': 'skin health', 'solution': 'GHK peptide', 'constraints': '', 'smiles': '', 'countries': ['US']}
worker = create_uspto_search_worker(data)
print(f'Worker created: {type(worker).__name__}')
"
   ```
</verification>

<success_criteria>
- perform_uspto_search function with 4-step progress reporting
- create_uspto_search_worker factory creates Worker from InputPanel data
- Worker checks is_cancelled() between steps
- ResultsPanel displays patent title, count, abstract tooltip
- ResultsPanel has patentSelected signal
- ResultsPanel has set_loading, set_error, clear methods
- All exports available from workers and widgets packages
</success_criteria>

<output>
After completion, create `.planning/phases/03-uspto-patent-search/03-02-SUMMARY.md`
</output>
